# Pure CSS Scroll-Driven Animations

This submission implements lightweight CSS utilities for scroll-driven animations using the modern `animation-timeline` specification.

## Utilities
- `.timeline-scroll`: Tracks overall page or container scrolling.
- `.timeline-view`: Tracks element visibility context inside the viewport.